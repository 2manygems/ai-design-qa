import type { GuidelineRule } from '../../types/guideline'

// Phase 3에서 타이포그래피 비교 로직을 구현합니다.
export const typographyRule: GuidelineRule = {
  ruleId: 'typography-not-in-guideline',
  check: () => [],
}
